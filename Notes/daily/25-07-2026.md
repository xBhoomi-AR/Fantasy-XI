# 25-07-2026

Worked on setting up generic LSTM Pipeline for final multi season data set

Built a configurable dataset struc and lstm arch w separate historical seq and target-gw context inputs

Added training/validation loop, early stopping, model checkpointing, eval metrics, experiment logging etc

Also created a reusable experiment runner and successfully tested the complete pipeline using dummy data


The core lstm infra is ready, only waiting for the final data set
further work would be dataset inpection, preprocessing, feature seq selecn 